[package]
name = "ethereum-adapter"
version.workspace = true
edition = "2021"

[dependencies]
risc0-zkvm = { version = "1.2.0", default-features = false, features = ['std'] }
nexus-core = { path = "../../../../core", default-features = false, features = [
    "zkvm-risc0",
] }
ethereum-adapter-core = { path = "../../ethereum_adapter_core", default-features = false, features = ["zkvm-risc0"] }